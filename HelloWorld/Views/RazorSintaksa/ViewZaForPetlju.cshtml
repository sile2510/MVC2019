@model string[]
@{
    ViewBag.Title = "ViewZaForPetlju";
}

<h2>ViewZaForPetlju</h2>
<ul>
    @foreach (var item in Model)
    {
        <li>@item</li>

    }

</ul>


<hr />
<ul>
    @for (int i = 0; i < Model.Length; i++)
    {
        <li>@Model[i]</li>

    }
</ul>

@{ 
    int j = Model.Length-1;
}

<ul>
    @while (j >= 0)
    {
        <li>@Model[j]</li>
        j--;
    }
</ul>


<h3>Ispis pomoću while</h3>
<ul>
    @for (int i = 0; i < Model.Length; i++)
    {
        <li>@Model[i]</li>

    }
</ul>


<h3>Ispis pomoću while NAPRIJED</h3>
@{
    int k = 0;
}

<ul>
    @while (k < Model.Length)
    {
        <li>@Model[k]</li>
        k++;
    }
</ul>